pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Welcome Jenkins Environment"
                sh 'echo using shell bu command from Jenkinsfile'
                echo 'not using shell but in the Jenkinsfile'
            }
        }    
    }
}